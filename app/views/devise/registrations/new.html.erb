<body class="body-auth">
  <div class="login-container">
    <h1>🚀 新規登録 🚀</h1>
    <p>HabiLogに登録して、習慣を楽しく管理しましょう！</p>

    <%= form_with model: resource, as: resource_name, url: registration_path(resource_name), local: true, class: "login-form" do |f| %>
      <div class="form-group">
        <%= f.label :nickname, "ニックネーム", class: "form-label" %>
        <%= f.text_field :nickname, autofocus: true, class: "form-input" %>
      </div>

      <div class="form-group">
        <%= f.label :email, "メールアドレス", class: "form-label" %>
        <%= f.email_field :email, class: "form-input" %>
      </div>

      <div class="form-group">
        <%= f.label :password, "パスワード", class: "form-label" %>
        <%= f.password_field :password, autocomplete: "new-password", class: "form-input" %>
      </div>

      <div class="form-actions">
        <%= f.submit "登録する", class: "btn" %>
      </div>
    <% end %>

    <p>すでにアカウントをお持ちですか？ <%= link_to "ログインはこちら", new_user_session_path, class: "link" %></p>
  </div>
</body>